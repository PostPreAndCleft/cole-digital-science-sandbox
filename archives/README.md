# Archives

This folder stores dated snapshots of public pages for reference.

Snapshots are created by `scripts/archive_pages.py`, which copies the target
HTML and rewrites relative links so the archived file works from this subfolder.

Example output:
- `archives/linkedin-2026-02-07.html`
